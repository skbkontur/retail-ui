.cache_common: &cache_common
  key:
    files:
      - yarn.lock
      - '**/package.json'
    prefix: $CACHE_PREFIX
  paths:
    - '**/node_modules/'

.cache_push:
  cache:
    <<: *cache_common
    policy: push

.cache_pull:
  cache:
    <<: *cache_common
    policy: pull
