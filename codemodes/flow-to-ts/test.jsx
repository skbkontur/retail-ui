// @noflow
import { type a, type z } from 'ZZZ';

var foo = 1;

var a: ?Node = a;
var b: Aaa = a;
var z: B<A> = a;
var y: B<?A> = a;
var y: React.Node = a;
var a: S<> = b;
var a: * = b;
type a = { +a: a };
type a = A => void;
type a = A => void;
// const a: [?string, a];
// var a = x?.x?.x;

function A<T: A>() {}
function A<T: A, B: U>() {}
const a = (1: number);

type A = {| a: number |};

export function a<T: {}>(a: A<T, ?Array<string>>, b?: (x: A1) => ?A2): Z {}

function aaa(): ?N {}

export function foo(values: (?string)[]): string {}

class A<B: C, X: Y = Z> {}

export interface IZ<-TValue> {}

const a = <T>(a: T): XX<T> => new XX();

export type { Xxxx };

export interface Z {
  foor(): Array<string>;
  bar(): ?React.ComponentType<EditorBaseProps<any>>
}

export interface IMapper<T> {
  parse(parsedQueryString: QueryObject): ?T;
  stringify(parsedQueryString: QueryObject, value: ?T): QueryObject
}

export class ReactKonturMetrika {
  unlistenFromHistory: ?() => void;
  previousPath: ?string;
}

function renderContact(
  title: string,
  person: ?ContactInfoModel
): (?React.Node)[] | null {}

type PortalBillsWidget = {
  open(PortalWidgetOpenProps): void
};

function zzz(limit: ?number = null): (?number)[] {}
