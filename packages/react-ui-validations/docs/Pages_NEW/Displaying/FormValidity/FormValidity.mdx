import { Canvas, Meta, Source, Stories, Story } from '@storybook/blocks';
import * as FormStories from './FormValidity.docs.stories';

<Meta of={FormStories} />

# Валидность формы

Чтобы провалидировать форму и узнать результат валидации, нужно вызвать метод `validate` на компоненте `ValidationContainer`.
Метод `validate` асинхронный, возвращает `Promise<boolean>`.

```jsx static
    <ValidationContainer ref={this.сontainer}>
      //...
    </ValidationContainer>

    <Button use={'primary'} onClick={this.handleSubmit}>Submit</Button>

    handleSubmit = async () => {
      const isValid = await this.container.validate();
      //...
    };
```

### Пример

<Canvas story={FormStories.Example1} withToolbar sourceState="shown" />

<Canvas of={FormStories.Example1} withToolbar sourceState="shown" />
<Canvas>
  <Story story={FormStories.Example1} withToolbar sourceState="shown" />
</Canvas>

<Stories />

<Source of={FormStories.Example1} />
