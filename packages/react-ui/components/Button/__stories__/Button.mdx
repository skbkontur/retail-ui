import * as ButtonStories from './Button.stories.tsx';
import { Meta, Primary, Controls, Canvas, Description } from '@storybook/blocks';
import { Button } from '../Button';
import { Gapped } from '../../Gapped';
import { MinusCircleIcon16Light, XIcon16Regular } from '@skbkontur/icons';

<Meta of={ButtonStories} />

# Button

[Компонент в Контур.Гайдах](https://guides.kontur.ru/components/button)

[Компонент в Figma](https://www.figma.com/file/87ScqxPzJpF9DcVBNYWOjM/%E2%9A%A1-Kontur-UI-Modern-Layout?node-id=200%3A2498)

<Description />

## Базовый пример

<Primary />

## Пропы

<Controls />

## Примеры использования

### Различные стили

<Canvas>
  <Gapped vertical>
    <Gapped>
      <Button use="default">Default</Button>
      <Button use="primary">Primary</Button>
      <Button use="success">Success</Button>
      <Button use="danger">Danger</Button>
      <Button use="pay">Pay</Button>
      <Button use="text">Text</Button>
      <Button use="backless">Backless</Button>
      <Button use="link">Link</Button>
    </Gapped>
  </Gapped>
</Canvas>

```jsx harmony
import { Gapped } from '@skbkontur/react-ui';

const bgStyle = {
  backgroundImage: `linear-gradient(to right, rgba(130, 130, 130, 0.5) 1px, transparent 1px),
    linear-gradient(to bottom, rgba(130, 130, 130, 0.5) 1px, transparent 1px)`,
  backgroundSize: `16px 16px`,
  backgroundPosition: `-8px -8px`,
  padding: 16,
};

<Gapped vertical>
  <Gapped>
    <Button use="default">Default</Button>
    <Button use="primary">Primary</Button>
    <Button use="success">Success</Button>
    <Button use="danger">Danger</Button>
    <Button use="pay">Pay</Button>
    <Button use="text">Text</Button>
    <Button use="backless">Backless</Button>
    <Button use="link">Link</Button>
  </Gapped>
  <Gapped style={bgStyle}>
    <Button use="default">Default</Button>
    <Button use="primary">Primary</Button>
    <Button use="success">Success</Button>
    <Button use="danger">Danger</Button>
    <Button use="pay">Pay</Button>
    <Button use="text">Text</Button>
    <Button use="backless">Backless</Button>
    <Button use="link">Link</Button>
  </Gapped>
</Gapped>;
```

### Иконка

<Canvas>
  <Gapped gap={5}>
    <Button icon={<XIcon16Regular />}>Закрыть</Button>
    <Button icon={<XIcon16Regular />} rightIcon={<XIcon16Regular />}>
      Закрыть
    </Button>
    <Button rightIcon={<XIcon16Regular />}>Закрыть</Button>
  </Gapped>
</Canvas>

### Размер

<Canvas>
  <div style={{ display: 'flex', alignItems: 'end', gap: '10px' }}>
    <Button size="small">Маленькая</Button>
    <Button size="medium">Средняя</Button>
    <Button size="large">Большая</Button>
  </div>
</Canvas>

### Ширина

<Canvas>
  <Button width={40}>Закрыть</Button>
</Canvas>

### Состояние валидации

<Canvas>
  <Button warning>Закрыть</Button>
  <Button error>Закрыть</Button>
</Canvas>

### Стрелка

<Canvas>
  <Gapped gap={5}>
    <Button arrow="left" size="medium">
      Назад
    </Button>
    <Button arrow size="medium">
      Далее
    </Button>
  </Gapped>
</Canvas>

### Состояние загрузки

**Поведение:**
Кнопка на время нахождения в состоянии загрузки отключается.
Если в кнопке есть иконка, на время загрузки иконка заменяется на спиннер, если иконки нет — весь контент кнопки заменяется на спиннер. Когда иконки две — заменяется только левая.

<Canvas>
  <Gapped>
    <Button width={150} loading={true}>
      Удалить
    </Button>
    <Button icon={<MinusCircleIcon16Light />} width={150} loading={true}>
      Удалить
    </Button>
    <Button rightIcon={<MinusCircleIcon16Light />} width={150} loading={true}>
      Удалить
    </Button>
    <Button icon={<MinusCircleIcon16Light />} rightIcon={<MinusCircleIcon16Light />} width={150} loading={true}>
      Удалить
    </Button>
  </Gapped>
</Canvas>

### Проп темы

<Canvas>
  <Gapped>
    <Button theme={{ textColorDefault: '#C00000' }}>Ok</Button>
    <Button use="link" theme={{ linkColor: '#C00000' }}>
      Ok
    </Button>
    <Button>Ok</Button>
  </Gapped>
</Canvas>

# h1

## h2

### h3

#### h4

##### h5

###### h6

текст
