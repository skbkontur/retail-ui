import * as MiniModalFooterStories from './MiniModalFooter.docs.stories.tsx';
import { Description, Primary, Stories } from '@storybook/blocks';
import { Meta } from '../../../.storybook-docs/Meta';
import { PropsTable } from '../../../.storybook-docs/PropsTable';

<Meta of={MiniModalFooterStories} />

# MiniModal.Footer

```jsx
import { MiniModal } from '@skbkontur/react-ui';

<MiniModal>
  ...
  <MiniModal.Footer>
    <Button size="medium">Действие</Button>
  </MiniModal.Footer>
</MiniModal>;
```

<Description />

Обёртка над [Modal.Footer](https://tech.skbkontur.ru/kontur-ui/?path=/docs/react-ui_overlay-modal-modalfooter--docs) с раскладкой кнопок мини-модалки. Используется только внутри [MiniModal](https://tech.skbkontur.ru/kontur-ui/?path=/docs/react-ui_overlay-minimodal-minimodal--docs).

<Primary />

## Все пропсы

<PropsTable />

## Использование

Проп `direction="row"` задаёт горизонтальную раскладку только при ровно двух дочерних элементах. Если дочерний элемент один или их больше двух, используется column (независимо от `direction`).

По гайду дизайна кнопки в мини-модалке — `size="medium"`. Другой размер — отступление, согласуйте с дизайнером. Между группами действий во футере можно вставить `MiniModal.Indent`.

Подсчёт `Children` для раскладки — через `React.Children.count`: одна обёртка (например Fragment вокруг двух кнопок) считается одним ребёнком. Подробнее — в разделе «MiniModal.Footer, React.Children и Fragment» на странице [MiniModal](https://tech.skbkontur.ru/kontur-ui/?path=/docs/react-ui_overlay-minimodal-minimodal--docs).

Наследуемые пропсы (например `sticky`, `panel`) — в [Modal.Footer](https://tech.skbkontur.ru/kontur-ui/?path=/docs/react-ui_overlay-modal-modalfooter--docs).

## Примеры

<Stories title="" includePrimary={false} />
