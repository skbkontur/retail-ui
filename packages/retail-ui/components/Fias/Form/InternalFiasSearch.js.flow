/* @flow */
import type { Address } from '../models/Address';
import type { FiasLocale } from '../locale/types';

type InternalFiasSearchProps = {|
  source: (query: string) => Promise<Address[]>,
  address: Address,
  onChange: (value: Address) => void,
  limit?: number,
  locale?: FiasLocale,
|};

declare export class InternalFiasSearch extends React$Component<InternalFiasSearchProps> {}

export default InternalFiasSearch;
