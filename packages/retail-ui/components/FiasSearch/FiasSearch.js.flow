/* @flow */
import type { FiasValue, APIProvider } from '../Fias/types';
import type { FiasLocale } from '../Fias/locale/types';
import { Address } from '../Fias/models/Address';

export type FiasSearchProps = {|
  value?: FiasValue,
  onChange?: (value: FiasValue) => void,
  width?: number | string,
  error?: boolean,
  warning?: boolean,
  baseUrl?: string,
  api?: APIProvider,
  limit?: number,
  locale?: FiasLocale,
  version?: string,
|};

export type FiasSearchState = {|
  address: Address,
  locale: FiasLocale,
|};

declare export class FiasSearch extends React$Component<FiasSearchProps, FiasSearchState> {}

export default FiasSearch;
