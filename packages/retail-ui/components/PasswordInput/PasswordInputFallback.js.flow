/* @flow */
import Input, { type InputProps } from '../Input';

export type PasswordInputFallbackProps = {|
  ...$Exact<InputProps>,
  visible?: boolean,
  size?: string,
  refInput: (element: Input) => void,
|};

declare class PasswordInputFallback extends React$Component<PasswordInputFallbackProps> {}

export default PasswordInputFallback;
