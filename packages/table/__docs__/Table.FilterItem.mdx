import { ArgTypes, Story } from '@storybook/blocks';
import { Meta } from '../../../.storybook-docs/Meta';
import { Table } from '../src/components/Table/Table';
import * as TableFilterItemStories from './Table.FilterItem.docs.stories';

<Meta of={TableFilterItemStories} />

# Table.FilterItem

Элемент списка для выбора значения в фильтре.

Компонент является обёрткой над `MenuItem` из `@skbkontur/react-ui` и принимает все его пропсы.

## Пример

<Story of={TableFilterItemStories.Basic} />

## Пропсы

<ArgTypes of={Table.FilterItem} />
