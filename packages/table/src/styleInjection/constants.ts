/**
 * Имя cascade layer'а, в который заворачивается CSS пакета. Слоевые стили
 * проигрывают unlayered-стилям потребителя независимо от порядка подключения —
 * это возвращает consumer-оверрайдам приоритет, утраченный при переходе на
 * adoptedStyleSheets (они каскадируются после обычных <style>/<link>).
 *
 * Побочный эффект (осознанный): unlayered-CSS выигрывает у слоя ВСЕГДА, поэтому
 * любые глобальные сбросы потребителя (`td {}`, `* {}`) и unlayered-стили peer-
 * библиотек перебивают базовые стили таблицы независимо от специфичности.
 */
export const TABLE_STYLE_LAYER = 'skbkontur-table';
